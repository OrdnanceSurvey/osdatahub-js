import {describe, expect, test} from '@jest/globals';
import { osfetch } from '../src/index'

describe("osfetch", () => {
    test("My First Test", () => {
        expect(5 + 5).toEqual(10)
    });
});
