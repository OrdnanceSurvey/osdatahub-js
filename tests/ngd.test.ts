import {describe, expect, test} from '@jest/globals';
import ngd from '../build/ngd';

// note: only supports BBox 'findBy'

describe("BBox Endpoint", () => {
    test("BBox Endpoint Passes", () => {

    })
    test("BBox Endpoint Fails", () => {

    })
})